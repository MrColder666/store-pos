# Changelog

## v2.3.1 (2026-06-30)

### ✨ 新增：幸运大转盘

- **H5 互动转盘** — Canvas 绘制的 20 格幸运大转盘，支持点击/触摸旋转
- **概率透明** — 右侧饼图展示中奖概率分布，标语「中奖概率透明，无套路！」
- **奖项列表** — 8 种奖项按比例分布（薯/片/好吃/冰粉/代金券/9折券/再抽一次/谢谢惠顾）
- **集齐活动** — 集齐「薯」「片」「好吃」免费领 2 包薯片，限量 3 次
- **页面路由** — `/lucky`，底部 Tab 栏新增抽奖入口
- **API** — `GET/POST /api/lucky/count` 兑换次数计数

## v2.2.1 (2026-06-29)

### ✨ 新增：优惠券自定义商品

- **优惠券类型商品** — 创建商品时可勾选「优惠券」，价格自动设为 ¥0，面值由折扣字段定义
- **整体减价** — 优惠券面值直接抵扣订单总额
- **单品减价** — 优惠券面值抵扣购物车中指定商品
- **密码保护** — 加入购物车需输入密码（102938），定制作弹窗
- **百分比/固定双模式** — 支持 20% off 和减 ¥3 两种方式，结算时准确计算
- **预建单结算** — 点击「去结算」立即调用后端建单，支付弹窗显示真实总价

### 🐛 修复
- 优惠券百分比在结算时正确计算（如 20% off ¥5 = ¥4）
- 商品编辑时折扣类型正确恢复（toggleCoupon 不再强制覆盖）

## v2.2.0 (2026-06-28)

### ✨ 新增：选项组多选 + 订单完成状态

- **多选选项组** — 商品选项组支持配置「允许多选」，顾客可同时勾选多个选项
  - 示例：冰粉选购小料时可同时加珍珠 + 椰果 + 红豆
- **选项编辑器更新** — 每个选项组新增「允许多选」复选框
- **收银台交互优化** — 多选组点击切换选中/取消（独立操作），单选组保持互斥
- **价格正确累加** — 多选中所有选项加价累加计入总价
- **订单详情显示** — 商品名附所有已选项（如「冰粉 (大杯, 珍珠, 红豆)」）

#### 🎁 订单「已完成」状态
- **新流程**: 待支付 → 已支付 → **已完成**（店员交付商品后标记）
- **订单管理页**: 新增「已完成」筛选、状态徽章、「完成交付」按钮
- **统计数据**: 已完成订单计入营收统计

### 🔧 数据库
- `product_options` 新增 `multi_select` 列（INTEGER DEFAULT 0）
- `orders.status` CHECK 约束新增 `'completed'`
- 自动迁移兼容旧数据

### 🐛 修复
- **订单「立即收款」无法点击「确认收款」** — `payNow()` 中 `closeDetail()` 先清空了 `detailOrderId`，导致异步加载订单详情时获取到 null，确认按钮一直处于 disabled 状态；改为先保存 `detailOrderId` 再关闭详情弹窗

## v2.1.0 (2026-06-22)

### ✨ 增强折扣系统 + 高端现代 UI 升级

#### 🏷️ 双模式折扣
- **百分比折扣支持小数** — 现在可以输入 12.5% 等含小数的百分比折扣
- **固定减价折扣** — 新增「固定减价」模式，直接输入减少的金额（如 -¥3.00）
- **折扣方式选择** — 商品编辑时可选择「百分比 (%)」或「固定减价 (¥)」
- **智能标签** — 收银台和商品列表自动识别折扣类型，显示正确标签（-12.5% 或 -¥3.00）

#### 🎨 高端现代 UI 重设计
- **全新视觉系统** — 深紫渐变主题、毛玻璃效果、精细阴影与圆角
- **沉浸式配色** — 从 `#1e1b4b` 到 `#4c1d95` 的深邃渐变头部，紫色主色调搭配金色强调色
- **流畅动效** — 卡片 hover 缩放、模态框弹性滑入、淡入淡出遮罩、按钮按下反馈
- **毛玻璃 Tab Bar** — 底部导航使用 backdrop-filter 毛玻璃效果，固定定位
- **精细化排版** — 优化字距、行高、字号层级，增加文字阴影和光晕效果
- **响应式布局** — 适配小屏设备（<380px）时自动调整布局

#### 🔧 其他改进
- `discount` 字段改为 REAL 类型，支持小数折扣值
- 新增 `discount_type` 字段（percent/fixed）
- 产品 CSV 导出新增折扣类型列
- 数据库自动迁移，旧数据兼容
- **修复商品选项加价不计入订单总价** — 前端购物车和建单 API 均正确传递选项价格，后端总价计算包含选项加价；订单详情显示含选项名的商品信息（如「拿铁 (大杯, 少糖)」）

## v2.0.0 (2026-06-20)

### ✨ 收费系统 — 真正的收银流程

#### 💳 支付系统
- **支付方式选择** — 结算时选择 纸币 / 微信 / 支付宝
- **二维码展示** — 微信/支付宝选择后显示收款码图片（可替换 `static/qr/` 下的 PNG）
- **现金找零** — 纸币模式下输入实收金额，自动计算找零
- **支付成功凭条** — 支付成功后显示详细凭条（商品、金额、支付方式、找零）
- **支付记录追踪** — 新增 `payments` 表记录每笔支付明细

#### 🔄 新收银流程
```
选择商品 → 去结算 → 选择支付方式 → 收款 → 凭条
                      ├── 纸币：输入实收→找零
                      ├── 微信：展示收款码→确认收款
                      └── 支付宝：展示收款码→确认收款
```

#### 📊 统计增强
- 支付方式分布：纸币/微信/支付宝各自营收
- 待支付订单提醒
- 订单列表显示支付方式和状态

#### 🗄 数据库
- `orders` 表新增 `payment_status`, `paid_amount`, `change_amount`, `payment_method`
- 新建 `payments` 表（id, order_id, method, amount, created_at）
- 支持未支付(pending)、已支付(paid)、已退款(refunded)、已取消(cancelled) 四种状态
- 向后兼容 v1.x 数据自动迁移

#### 📥 CSV 导出
- 订单导出新增支付方式、支付状态、实收、找零字段

## v1.3.0 (2026-06-19)

### ✨ 新增
- **🖼️ 图表导出 PNG** — 每周营收柱状图一键导出为图片，可保存/分享

## v1.2.1 (2026-06-19)

### 🐛 修复
- 修复商品管理页面 JavaScript 函数声明缺失导致页面瘫痪

## v1.2.0 (2026-06-19)

### ✨ 新增功能
- **⚙️ 商品选项** — 创建商品时可配置选项组（如尺寸、甜度），每个选项可单独加价
- **🛒 选项选择弹窗** — 收银台点击带选项商品时弹出选择界面，选完再入购物车
- **📝 商品折扣** — 商品可设置 0-100% 折扣，收银台自动计算折后价并显示折扣标签
- **✏️ 编辑订单备注** — 在订单详情弹窗中直接修改备注
- **🗑️ 删除订单** — 永久删除订单，自动恢复库存，有确认弹窗防止误操作

### 🔧 改进
- 统一使用自定义确认弹窗替代原生 confirm()（解决内嵌浏览器不支持问题）
- 购物车显示已选选项信息
- 种子数据包含折扣和备注示例

## v1.0.0 (2026-06-11)

### ✨ 功能
- 🧾 **收银台** — 搜索添加商品、购物车、结算出单，自动扣减库存
- 📦 **商品管理** — 增删改商品（名称、价格、分类、库存）
- 📋 **订单管理** — 历史订单列表、详情查看、退款（自动恢复库存）
- 📊 **今日统计** — 今日营收、订单数、售出件数、热销排行
- 📈 **每周收益明细** — 每日营收柱状图、时段分布图、每日明细表、订单时间线
- 📥 **CSV 导出** — 商品、订单、每周数据一键导出

### 🛠 技术栈
- 后端: Python / Flask
- 数据库: SQLite
- 前端: 原生 HTML + CSS + JavaScript
