# 🎟 抽奖纸条 UUID 使用指南

## 概述

每张抽奖纸条附带一个全局唯一的 UUID（v4），用于核销验证。系统通过 UUID 确保每张纸条只能兑换一次。

## 纸条内容

```
┌──────────────────┐
│      薯          │  ← 奖项（薯 / 片 / 好吃）
│                  │
│ a1b2c3d4-e5f6   │  ← UUID 前缀（前 12 位，便于输入）
└──────────────────┘
```

纸条下方完整 UUID 示例：
```
c46c953c-4aeb-460a-8be4-3cdc9edf88c0
```

## 核销流程

### 方式一：手动输入

1. 打开抽奖兑换页 → 「🔍 核销纸条」
2. 输入纸条上的 UUID（完整或前缀 ≥ 8 位）
3. 点击「兑换」

**输入规则**：

| 输入 | 结果 |
|------|------|
| `c46c953c-4aeb-460a-8be4-3cdc9edf88c0` | ✅ 完整匹配 |
| `c46c953c-4ae` | ✅ 前缀匹配（≥ 8 位） |
| `c46c` | ❌ 前缀太短，无法唯一匹配 |
| `xyz123` | ❌ 无效（不存在） |

### 方式二：扫码输入（推荐）

使用手机/扫码枪扫描纸条上的 UUID 条形码或二维码，自动填入输入框。

### 兑换结果

| 状态 | 提示 |
|------|------|
| 未使用 | 🎉 兑换成功：薯 |
| 已被兑过 | ❌ 已兑过 — 该纸条已在 [时间] 被兑换 |
| 不存在 | ❌ 无效 UUID — 该纸条不存在 |

## 生成纸条

### 管理端

1. 抽奖页面 → 管理员面板（➕）→ 「🎟 抽奖兑换工具」
2. 设置数量 → 「生成纸条」
3. 点击「🖨 打印」→ 浏览器打印 → 另存为 PDF 或直接打印

### API 生成

```bash
# 生成 20 张纸条
curl -X POST http://127.0.0.1:5000/api/tickets/generate \
  -H 'Content-Type: application/json' \
  -d '{"count": 20}'

# 返回示例
{"ok":true, "count":20, "tickets":[
  {"uuid":"c46c953c-4aeb-460a-8be4-3cdc9edf88c0","type":"薯"},
  {"uuid":"525fd720-53c5-42af-848a-95a6bbee04dd","type":"片"},
  ...
]}
```

## 数据持久化

所有纸条记录存储在 `store.db` 的 `tickets` 表中：

```sql
CREATE TABLE tickets (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    type TEXT NOT NULL,  -- '薯' | '片' | '好吃'
    used INTEGER DEFAULT 0,  -- 0=未兑, 1=已兑
    created_at TEXT
);
```

## 管理操作

| 操作 | 说明 |
|------|------|
| 生成纸条 | 一次性生成指定数量，奖品均匀分布 |
| 核销纸条 | 验证 UUID 并标记已使用 |
| 清空全部 | 删除所有纸条记录（不可恢复） |

## 安全说明

- UUID v4 碰撞概率极低（~10³⁷ 分之一），无需担心重复
- 每张纸条仅能兑换一次，防止重复使用
- 核销记录不可逆，确保公平性
